#!/bin/sh

# Eventually, this script will load the image configs from the IDAs.
# For now, the image configs exist in this repo,
# so this script is a no-op.

echo "load-idas.sh: Nothing to do; all set."
